"use client";

import Link from "next/link";
import { useEffect, useLayoutEffect,useRef, useState } from "react";
import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

export default function Home() {
  const mainRef = useRef<HTMLDivElement>(null);
  const sectionRef = useRef<HTMLDivElement>(null);
  const morphShapeRef = useRef<HTMLDivElement>(null);
  const morphTitleRef = useRef<HTMLDivElement>(null);
  const cardsTrackRef = useRef<HTMLDivElement>(null);
  const [menuOpen, setMenuOpen] = useState(false);
  const [billing, setBilling] = useState<"monthly" | "yearly">("monthly");
  const [selectedPlan, setSelectedPlan] = useState("Growth");
  const [demoOpen, setDemoOpen] = useState(false);
  const [activeFaq, setActiveFaq] = useState<number | null>(0);
  const [activeTestimonial, setActiveTestimonial] = useState(0);
  const [toast, setToast] = useState<string | null>(null);

  const showToast = (msg: string) => {
    setToast(msg);
    setTimeout(() => setToast(null), 2500);
  };

  // ---------- Ripple effect on any .ripple-btn click ----------
  const createRipple = (e: React.MouseEvent<HTMLButtonElement>) => {
    const btn = e.currentTarget;
    const circle = document.createElement("span");
    const diameter = Math.max(btn.clientWidth, btn.clientHeight);
    const rect = btn.getBoundingClientRect();
    circle.style.width = circle.style.height = `${diameter}px`;
    circle.style.left = `${e.clientX - rect.left - diameter / 2}px`;
    circle.style.top = `${e.clientY - rect.top - diameter / 2}px`;
    circle.classList.add("ripple");
    btn.appendChild(circle);
    setTimeout(() => circle.remove(), 650);
  };

  // ---------- Magnetic button hover ----------
  const magnetize = (e: React.MouseEvent<HTMLElement>) => {
    const el = e.currentTarget;
    const rect = el.getBoundingClientRect();
    const x = e.clientX - rect.left - rect.width / 2;
    const y = e.clientY - rect.top - rect.height / 2;
    gsap.to(el, {
      x: x * 0.25,
      y: y * 0.25,
      duration: 0.4,
      ease: "power2.out",
    });
  };
  const resetMagnet = (e: React.MouseEvent<HTMLElement>) => {
    gsap.to(e.currentTarget, {
      x: 0,
      y: 0,
      duration: 0.5,
      ease: "elastic.out(1,0.4)",
    });
  };

  // ---------- 3D tilt on cards ----------
  const tiltCard = (e: React.MouseEvent<HTMLDivElement>) => {
    const card = e.currentTarget;
    const rect = card.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const y = e.clientY - rect.top;
    const rotateX = ((y - rect.height / 2) / rect.height) * -12;
    const rotateY = ((x - rect.width / 2) / rect.width) * 12;
    gsap.to(card, {
      rotateX,
      rotateY,
      scale: 1.03,
      duration: 0.4,
      ease: "power2.out",
      transformPerspective: 800,
    });
  };
  const resetTilt = (e: React.MouseEvent<HTMLDivElement>) => {
    gsap.to(e.currentTarget, {
      rotateX: 0,
      rotateY: 0,
      scale: 1,
      duration: 0.6,
      ease: "power3.out",
    });
  };

useLayoutEffect(() => {
  gsap.registerPlugin(ScrollTrigger);

  const ctx = gsap.context(() => {
    // ============ NAVBAR SCROLL CLASS ============
    ScrollTrigger.create({
      start: 50,
      end: 99999,
      toggleClass: { targets: ".navbar-glass", className: "scrolled" },
    });

    // ============ SCROLL PROGRESS BAR ============
    gsap.to(".scroll-progress", {
      scaleX: 1,
      ease: "none",
      scrollTrigger: {
        trigger: document.body,
        start: "top top",
        end: "bottom bottom",
        scrub: 0.3,
      },
    });

    // ============ HERO ENTRANCE ============
    const heroTl = gsap.timeline({ defaults: { ease: "power3.out" } });
    heroTl
      .from(".hero-badge", { y: 30, opacity: 0, duration: 0.7 })
      .from(
        ".hero-title-word",
        { y: 60, opacity: 0, duration: 0.8, stagger: 0.06 },
        "-=0.4",
      )
      .from(".hero-sub", { y: 30, opacity: 0, duration: 0.7 }, "-=0.4")
      .from(
        ".hero-cta-group > *",
        { y: 20, opacity: 0, duration: 0.6, stagger: 0.15 },
        "-=0.4",
      )
      .from(
        ".hero-mockup",
        { x: 80, opacity: 0, duration: 1, ease: "power4.out" },
        "-=0.8",
      )
      .from(
        ".hero-mini-card",
        { scale: 0.7, opacity: 0, duration: 0.6, ease: "back.out(1.7)" },
        "-=0.3",
      );

    // ============ MOUSE PARALLAX BLOBS ============
    const hero = document.querySelector(".hero-section");
    const blobs = gsap.utils.toArray<HTMLElement>(".blob");
    const handleMouseMove = (e: MouseEvent) => {
      const { innerWidth, innerHeight } = window;
      const x = (e.clientX / innerWidth - 0.5) * 40;
      const y = (e.clientY / innerHeight - 0.5) * 40;
      blobs.forEach((blob, i) => {
        gsap.to(blob, {
          x: x * (i + 1) * 0.5,
          y: y * (i + 1) * 0.5,
          duration: 1.2,
          ease: "power2.out",
        });
      });
    };
    hero?.addEventListener("mousemove", handleMouseMove as EventListener);

    gsap.to(".blob-1", {
      yPercent: 60,
      scrollTrigger: {
        trigger: ".hero-section",
        start: "top top",
        end: "bottom top",
        scrub: 1,
      },
    });
    gsap.to(".blob-2", {
      yPercent: -50,
      scrollTrigger: {
        trigger: ".hero-section",
        start: "top top",
        end: "bottom top",
        scrub: 1,
      },
    });

    // ============ SCROLL-VELOCITY SKEW ============
    let proxy = { skew: 0 };
    let skewSetter = gsap.quickSetter(".skew-target", "skewY", "deg");
    let clamp = gsap.utils.clamp(-6, 6);
    ScrollTrigger.create({
      onUpdate: (self) => {
        let skew = clamp(self.getVelocity() / -300);
        if (Math.abs(skew) > Math.abs(proxy.skew)) {
          proxy.skew = skew;
          gsap.to(proxy, {
            skew: 0,
            duration: 0.8,
            ease: "power3",
            overwrite: true,
            onUpdate: () => skewSetter(proxy.skew),
          });
        }
      },
    });

    // ============ LOGO MARQUEE ============
    gsap.to(".marquee-track", {
      xPercent: -50,
      repeat: -1,
      duration: 18,
      ease: "linear",
    });

    // ============ GENERIC REVEAL ============
    gsap.utils.toArray<HTMLElement>(".reveal").forEach((el) => {
      gsap.from(el, {
        y: 60,
        opacity: 0,
        duration: 0.9,
        ease: "power3.out",
        scrollTrigger: { trigger: el, start: "top 85%" },
      });
    });

    gsap.utils.toArray<HTMLElement>(".stagger-group").forEach((group) => {
      gsap.from(group.children, {
        y: 50,
        opacity: 0,
        duration: 0.7,
        stagger: 0.15,
        ease: "power3.out",
        scrollTrigger: { trigger: group, start: "top 85%" },
      });
    });

    // ============ FEATURE ICON ROTATE-IN ============
    gsap.utils.toArray<HTMLElement>(".feature-icon").forEach((icon) => {
      gsap.from(icon, {
        rotate: -180,
        scale: 0,
        opacity: 0,
        duration: 0.8,
        ease: "back.out(1.8)",
        scrollTrigger: { trigger: icon, start: "top 88%" },
      });
    });

    // ============ ANIMATED COUNTERS ============
    gsap.utils.toArray<HTMLElement>(".counter").forEach((el) => {
      const target = parseFloat(el.dataset.target || "0");
      const obj = { val: 0 };
      ScrollTrigger.create({
        trigger: el,
        start: "top 90%",
        once: true,
        onEnter: () => {
          gsap.to(obj, {
            val: target,
            duration: 1.8,
            ease: "power2.out",
            onUpdate: () =>
              (el.textContent =
                Math.floor(obj.val).toString() + (el.dataset.suffix || "")),
          });
        },
      });
    });

    // ============ PRODUCT SHOWCASE ============
    gsap.from(".showcase-mockup", {
      scale: 0.85,
      opacity: 0,
      duration: 1,
      ease: "power3.out",
      scrollTrigger: { trigger: ".showcase-section", start: "top 70%" },
    });
    gsap.utils.toArray<HTMLElement>(".showcase-point").forEach((pt, i) => {
      gsap.from(pt, {
        x: -60,
        opacity: 0,
        duration: 0.7,
        delay: i * 0.15,
        ease: "power3.out",
        scrollTrigger: { trigger: pt, start: "top 85%" },
      });
    });

    // ============ HORIZONTAL TESTIMONIALS PIN ============
    const testimonialTrack = document.querySelector(
      ".testimonial-track",
    ) as HTMLElement;
    if (testimonialTrack) {
      const scrollAmount =
        testimonialTrack.scrollWidth - window.innerWidth + 100;
      gsap.to(testimonialTrack, {
        x: -scrollAmount,
        ease: "none",
        scrollTrigger: {
          trigger: ".testimonial-pin-section",
          start: "top top",
          end: () => `+=${scrollAmount + 400}`,
          scrub: 1,
          pin: true,
          pinType: "transform", // 🔧 FIX: viewport-fixed ki jagah element-relative pin
          anticipatePin: 1,
          invalidateOnRefresh: true,
        },
      });
    }

    // ============ PRICING CARDS ============
    gsap.from(".pricing-card", {
      y: 70,
      opacity: 0,
      scale: 0.95,
      duration: 0.8,
      stagger: 0.15,
      ease: "back.out(1.4)",
      scrollTrigger: { trigger: ".pricing-section", start: "top 75%" },
    });

    // ============ FAQ ITEMS ============
    gsap.utils.toArray<HTMLElement>(".faq-item").forEach((el, i) => {
      gsap.from(el, {
        y: 30,
        opacity: 0,
        duration: 0.6,
        delay: i * 0.08,
        ease: "power3.out",
        scrollTrigger: { trigger: el, start: "top 90%" },
      });
    });

    // ============ CTA CONTENT ============
    gsap.from(".cta-content > *", {
      y: 40,
      opacity: 0,
      duration: 0.8,
      stagger: 0.15,
      ease: "power3.out",
      scrollTrigger: { trigger: ".cta-section", start: "top 80%" },
    });

    // ============ PHONEPE-STYLE MORPH SECTION ============
    const section = sectionRef.current;
    const morph = morphShapeRef.current;
    const title = morphTitleRef.current;
    const track = cardsTrackRef.current;

    let marqueeTween: gsap.core.Tween | null = null;

    if (section && morph && title && track) {
      const getFinalWidth = () => Math.min(section.clientWidth * 0.92, 1100);
      const getFinalHeight = () => (window.innerWidth < 768 ? 380 : 520);
      const getHalfShift = () => track.scrollWidth / 2;

      gsap.set(morph, { width: 140, height: 140, borderRadius: "50%" });
      gsap.set(title, { opacity: 0, y: 30 });
      gsap.set(track, { opacity: 0, y: 40, x: 0 });

      const morphTl = gsap.timeline({
        scrollTrigger: {
          trigger: section,
          start: "top top",
          end: () => "+=" + window.innerHeight * 3,
          scrub: 1,
          pin: true,
          pinType: "transform", // 🔧 FIX: yehi asli fix hai overlap ke liye
          pinSpacing: true,
          anticipatePin: 1,
          invalidateOnRefresh: true,
          onLeave: () => startMarquee(),
          onEnterBack: () => stopMarquee(),
          onLeaveBack: () => stopMarquee(),
        },
      });

      morphTl
        .to(morph, {
          width: () => getFinalWidth(),
          height: () => getFinalHeight(),
          borderRadius: 32,
          duration: 1,
          ease: "power2.inOut",
        })
        .to(title, { opacity: 1, y: 0, duration: 0.5 }, "-=0.15")
        .to(track, { opacity: 1, y: 0, duration: 0.5 }, "<")
        .to(track, { x: () => -getHalfShift(), duration: 2, ease: "none" });

      function startMarquee() {
        if (marqueeTween || !track) return;
        const half = getHalfShift();
        gsap.set(track, { x: -half });
        marqueeTween = gsap.to(track, {
          x: -half * 2,
          duration: 16,
          ease: "linear",
          repeat: -1,
          modifiers: {
            x: (x) => {
              const val = parseFloat(x);
              return gsap.utils.wrap(-half * 2, -half, val) + "px";
            },
          },
        });
      }

      function stopMarquee() {
        if (marqueeTween) {
          marqueeTween.kill();
          marqueeTween = null;
        }
      }
    }

    // ============ RESIZE + LOAD REFRESH HANDLER ============
    let resizeTimer: ReturnType<typeof setTimeout>;
    const handleResize = () => {
      clearTimeout(resizeTimer);
      resizeTimer = setTimeout(() => ScrollTrigger.refresh(), 200);
    };
    window.addEventListener("resize", handleResize);

    // 🔧 FIX: images/fonts load hone ke baad measurements dobara refresh karo
    const handleWindowLoad = () => ScrollTrigger.refresh();
    window.addEventListener("load", handleWindowLoad);

    // 🔧 FIX: multiple delayed refreshes taaki async-loaded content bhi account ho
    const refreshTimer1 = setTimeout(() => ScrollTrigger.refresh(), 300);
    const refreshTimer2 = setTimeout(() => ScrollTrigger.refresh(), 1000);

    return () => {
      hero?.removeEventListener("mousemove", handleMouseMove as EventListener);
      window.removeEventListener("resize", handleResize);
      window.removeEventListener("load", handleWindowLoad);
      clearTimeout(refreshTimer1);
      clearTimeout(refreshTimer2);
      clearTimeout(resizeTimer);
      if (marqueeTween) marqueeTween.kill();
    };
  }, mainRef);

  return () => ctx.revert();
}, []);

  const features = [
    {
      title: "Employee Management",
      icon: "👥",
      desc: "Centralized employee database with role-based access.",
    },
    {
      title: "Payroll System",
      icon: "💸",
      desc: "Auto-calculate salaries, tax & deductions instantly.",
    },
    {
      title: "Attendance Tracking",
      icon: "🕒",
      desc: "Biometric, GPS & face-recognition check-ins.",
    },
    {
      title: "AI Chat Assistant",
      icon: "🤖",
      desc: "Ask HR queries, get instant AI-powered answers.",
    },
    {
      title: "Performance Tracking",
      icon: "📈",
      desc: "OKRs, reviews & growth insights in one place.",
    },
    {
      title: "Leave Management",
      icon: "🗓️",
      desc: "Smart approvals with balance auto-calculation.",
    },
  ];

 const steps = [
   {
     n: "01",
     title: "Sign Up",
     desc: "Create your workspace in under 2 minutes.",
   },
   {
     n: "02",
     title: "Add Your Team",
     desc: "Bulk import employees via CSV or API.",
   },
   {
     n: "03",
     title: "Integrate Tools",
     desc: "Connect Slack, email & existing HR systems.",
   },
   {
     n: "04",
     title: "Automate",
     desc: "Let AI handle payroll, leaves & attendance.",
   },
   {
     n: "05",
     title: "Analyze",
     desc: "Get AI-powered insights & performance reports.",
   },
   {
     n: "06",
     title: "Grow",
     desc: "Track performance and scale effortlessly.",
   },
 ];

  const testimonials = [
    {
      text: "Best HR tool I've used! Cut our admin work by 70%.",
      role: "HR Manager, TechCorp",
    },
    {
      text: "Amazing automation features, payroll runs itself now.",
      role: "Team Lead, Finlytics",
    },
    {
      text: "Saved our company hours every single week.",
      role: "Founder, Nimbus Labs",
    },
    {
      text: "The AI assistant answers HR queries instantly.",
      role: "COO, BrightWorks",
    },
    {
      text: "Onboarding went from 3 days to 30 minutes.",
      role: "HR Head, Vertex Inc",
    },
  ];

  const plans = [
    {
      name: "Starter",
      monthly: 0,
      yearly: 0,
      tag: "Up to 10 employees",
      features: ["Basic Payroll", "Attendance", "Email Support"],
    },
    {
      name: "Growth",
      monthly: 499,
      yearly: 399,
      tag: "Per employee/mo",
      features: [
        "Everything in Starter",
        "AI Assistant",
        "Performance Tracking",
        "Priority Support",
      ],
      popular: true,
    },
    {
      name: "Enterprise",
      monthly: null,
      yearly: null,
      tag: "Unlimited scale",
      features: [
        "Everything in Growth",
        "Dedicated Manager",
        "Custom Integrations",
        "SLA & Security",
      ],
    },
  ];

  const faqs = [
    {
      q: "Can I switch plans later?",
      a: "Yes, you can upgrade or downgrade your plan anytime from the dashboard — changes apply instantly.",
    },
    {
      q: "Is there a free trial?",
      a: "Yes! Every plan comes with a 14-day free trial, no credit card required.",
    },
    {
      q: "Does it support multiple locations?",
      a: "Absolutely. HRMS AI supports multi-branch, multi-country payroll and compliance out of the box.",
    },
    {
      q: "How secure is our data?",
      a: "We're SOC 2 compliant with end-to-end encryption and daily automated backups.",
    },
  ];

  const logos = [
    "Acme",
    "Nimbus",
    "Finlytics",
    "Vertex",
    "BrightWorks",
    "TechCorp",
    "Orbit",
    "Zenith",
  ];

  return (
    <div ref={mainRef}>
      <div className="scroll-progress" />

      {/* TOAST */}
      {toast && <div className="toast-box">{toast}</div>}

      {/* DEMO MODAL */}
      {demoOpen && (
        <div className="modal-backdrop" onClick={() => setDemoOpen(false)}>
          <div className="modal-box" onClick={(e) => e.stopPropagation()}>
            <button className="modal-close" onClick={() => setDemoOpen(false)}>
              ✕
            </button>
            <h4 className="fw-bold mb-3">🎬 Product Demo</h4>
            <div className="modal-video-placeholder">
              <span>▶</span>
              <p className="mt-2 mb-0 text-muted">Demo video would play here</p>
            </div>
            <p className="text-muted mt-3 mb-0">
              See how HRMS AI automates payroll, attendance & performance
              tracking in under 3 minutes.
            </p>
          </div>
        </div>
      )}

      {/* NAVBAR */}
      <nav className="navbar-glass navbar navbar-expand-lg fixed-top">
        <div className="container d-flex justify-content-between align-items-center py-2">
          {/* LOGO */}
          <div className="d-flex align-items-center gap-2 logo-group">
            <div className="logo-icon">
              <span>MAK</span>
            </div>
            <span className="fw-bold text-white fs-4 logo-text">
              HRMS<span className="gradient-text">.AI</span>
            </span>
          </div>

          {/* NAV LINKS */}
          <div className="d-none d-md-flex align-items-center gap-1 nav-links-wrap">
            {["Features", "How it Works", "Pricing", "FAQ"].map((t) => (
              <a
                key={t}
                href={`#${t.toLowerCase().replace(/\s/g, "")}`}
                className="nav-link-glow"
              >
                {t}
              </a>
            ))}
          </div>

          {/* RIGHT SIDE */}
          <div className="d-flex align-items-center gap-3">
            <Link href="/login" className="d-none d-lg-block">
              <span className="login-link">Login</span>
            </Link>
            <Link href="/login" className="d-none d-md-block">
              <button
                className="btn-glow-sm ripple-btn"
                onClick={createRipple}
                onMouseMove={magnetize}
                onMouseLeave={resetMagnet}
              >
                Get Started →
              </button>
            </Link>
            <button
              className="hamburger d-md-none"
              onClick={() => setMenuOpen(!menuOpen)}
            >
              <span className={menuOpen ? "bar bar1 open" : "bar bar1"} />
              <span className={menuOpen ? "bar bar2 open" : "bar bar2"} />
              <span className={menuOpen ? "bar bar3 open" : "bar bar3"} />
            </button>
          </div>
        </div>

        <div className={`mobile-menu ${menuOpen ? "open" : ""}`}>
          {["Features", "How it Works", "Pricing", "FAQ"].map((t) => (
            <a
              key={t}
              href={`#${t.toLowerCase().replace(/\s/g, "")}`}
              onClick={() => setMenuOpen(false)}
            >
              {t}
            </a>
          ))}
          <Link href="/login" onClick={() => setMenuOpen(false)}>
            <span className="mobile-login">Login</span>
          </Link>
          <div className="mobile-cta-wrap">
            <Link href="/login" onClick={() => setMenuOpen(false)}>
              <button className="btn-glow-sm w-100">Get Started →</button>
            </Link>
          </div>
        </div>
      </nav>

      {/* 1. HERO */}
      <section className="hero-section position-relative overflow-hidden text-white d-flex align-items-center">
        <div className="blob blob-1" />
        <div className="blob blob-2" />
        <div className="blob blob-3" />

        <div
          className="container position-relative"
          style={{ zIndex: 2, paddingTop: 60 }}
        >
          <div className="row align-items-center">
            <div className="col-lg-6">
              <span className="badge-pill hero-badge">
                ✨ AI-Powered HR Platform
              </span>
              <h1 className="hero-title mt-3">
                <span className="d-block">
                  {"HR Management,".split(" ").map((w, i) => (
                    <span
                      key={i}
                      className="hero-title-word d-inline-block me-2"
                    >
                      {w}
                    </span>
                  ))}
                </span>
                <span className="d-block gradient-text">
                  {"Reimagined with AI".split(" ").map((w, i) => (
                    <span
                      key={i}
                      className="hero-title-word d-inline-block me-2"
                    >
                      {w}
                    </span>
                  ))}
                </span>
              </h1>
              <p
                className="lead mt-3 hero-sub"
                style={{ color: "rgba(255,255,255,0.85)" }}
              >
                Payroll, attendance, performance & leave — all automated, all in
                one smart dashboard built for modern teams.
              </p>
              <div className="d-flex gap-3 mt-4 flex-wrap hero-cta-group">
                <Link href="/login">
                  <button
                    className="btn-glow ripple-btn"
                    onClick={createRipple}
                    onMouseMove={magnetize}
                    onMouseLeave={resetMagnet}
                  >
                    Get Started Free →
                  </button>
                </Link>
                <button
                  className="btn-outline-glow ripple-btn"
                  onClick={(e) => {
                    createRipple(e);
                    setDemoOpen(true);
                  }}
                  onMouseMove={magnetize}
                  onMouseLeave={resetMagnet}
                >
                  ▶ Watch Demo
                </button>
              </div>
            </div>

            <div className="col-lg-6 d-none d-lg-block">
              <div
                className="floating-card card-main hero-mockup"
                onMouseMove={tiltCard}
                onMouseLeave={resetTilt}
                style={{ transformStyle: "preserve-3d" }}
              >
                <div className="card-glow-header">
                  <div className="dot" style={{ background: "#ff5f57" }} />
                  <div className="dot" style={{ background: "#febc2e" }} />
                  <div className="dot" style={{ background: "#28c840" }} />
                </div>
                <div className="p-4">
                  <h6 className="text-dark mb-3">Attendance Overview</h6>
                  <div className="progress-bar-track mb-2">
                    <div
                      className="progress-bar-fill"
                      style={{ width: "88%" }}
                    />
                  </div>
                  <small className="text-muted">88% Present Today</small>
                  <div className="mini-stat mt-3">
                    <span>🤖 AI Assistant</span>
                    <span className="pulse-dot" />
                  </div>
                </div>
              </div>
              <div className="floating-card card-mini hero-mini-card">
                <div className="fw-bold" style={{ color: "#5f259f" }}>
                  ✅ Leave Approved
                </div>
                <small className="text-muted">2 mins ago</small>
              </div>
            </div>
          </div>
        </div>

        <svg
          className="wave-bottom"
          viewBox="0 0 1440 120"
          preserveAspectRatio="none"
        >
          <path
            fill="#ffffff"
            d="M0,64L80,58.7C160,53,320,43,480,48C640,53,800,75,960,80C1120,85,1280,75,1360,69.3L1440,64L1440,120L0,120Z"
          />
        </svg>
      </section>

      {/* 2. STATS */}
      <section className="container">
        {/* <div className="stats-strip row text-center g-4"> */}
        <div className="stats-strip row text-center g-3 g-md-4">
          <div className="col-6 col-md-3">
            <div
              className="counter fw-bold"
              data-target="500"
              data-suffix="+"
              style={{ fontSize: "2.75rem", color: "#5f259f" }}
            >
              0
            </div>
            <p className="text-muted mb-0">Companies</p>
          </div>
          <div className="col-6 col-md-3">
            <div
              className="counter fw-bold"
              data-target="50"
              data-suffix="k+"
              style={{ fontSize: "2.75rem", color: "#5f259f" }}
            >
              0
            </div>
            <p className="text-muted mb-0">Employees Managed</p>
          </div>
          <div className="col-6 col-md-3">
            <div
              className="counter fw-bold"
              data-target="99"
              data-suffix="%"
              style={{ fontSize: "2.75rem", color: "#5f259f" }}
            >
              0
            </div>
            <p className="text-muted mb-0">Uptime</p>
          </div>
          <div className="col-6 col-md-3">
            <div
              className="counter fw-bold"
              data-target="24"
              data-suffix="/7"
              style={{ fontSize: "2.75rem", color: "#5f259f" }}
            >
              0
            </div>
            <p className="text-muted mb-0">AI Support</p>
          </div>
        </div>
      </section>

      {/* 3. MARQUEE */}
      <section className="py-5">
        <p className="text-center text-muted reveal mb-4">
          TRUSTED BY FAST-GROWING TEAMS
        </p>
        <div className="marquee-wrap">
          <div className="marquee-track">
            {[...logos, ...logos].map((l, i) => (
              <span key={i} className="marquee-item">
                {l}
              </span>
            ))}
          </div>
        </div>
      </section>

      {/* 4. FEATURES */}
      <section id="features" className="container py-5 mt-4">
        <div className="text-center mb-5 reveal">
          <span className="section-tag">FEATURES</span>
          <h2 className="fw-bold mt-2">Everything Your HR Team Needs</h2>
          <p className="text-muted">One platform. Zero manual work.</p>
        </div>
        <div className="row g-4 stagger-group">
          {features.map((item, i) => (
            // <div key={i} className="col-md-4">
            <div
              key={i}
              className="col-md-4 stack-card-mobile"
              style={{ zIndex: i + 1 }}
            >
              <div
                className="feature-card h-100"
                onMouseMove={tiltCard}
                onMouseLeave={resetTilt}
                style={{ transformStyle: "preserve-3d" }}
              >
                <div className="feature-icon">{item.icon}</div>
                <h5 className="mt-3 fw-bold">{item.title}</h5>
                <p className="text-muted mb-0">{item.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* 5. HOW IT WORKS - PHONEPE STYLE CIRCLE-TO-SQUARE MORPH */}
      <section
        id="howitworks"
        ref={sectionRef}
        className="phonepe-section position-relative overflow-hidden"
      >
        <div className="phonepe-rings">
          <span className="ring ring-1" />
          <span className="ring ring-2" />
          <span className="ring ring-3" />
          <span className="ring ring-4" />
        </div>

        <div className="phonepe-pin-inner">
          <div ref={morphShapeRef} className="morph-shape">
            <div ref={morphTitleRef} className="morph-title">
              <span className="section-tag" style={{ color: "#ffd200" }}>
                PROCESS
              </span>
              <h2 className="fw-bold text-white mt-2">
                Get Started in 6 Simple Steps
              </h2>
            </div>

            <div className="cards-viewport">
              <div ref={cardsTrackRef} className="cards-track">
                {[...steps, ...steps].map((s, i) => (
                  <div key={i} className="phonepe-card">
                    <div className="phonepe-card-number">{s.n}</div>
                    <h6 className="fw-bold text-white mb-1 mt-2">{s.title}</h6>
                    <p className="phonepe-card-desc mb-0">{s.desc}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </section>
      {/* 6. PRODUCT SHOWCASE */}
      <section className="showcase-section container py-5 my-4">
        <div className="row align-items-center g-5">
          <div className="col-lg-6">
            <div
              className="floating-card showcase-mockup"
              style={{ maxWidth: 480, transformStyle: "preserve-3d" }}
              onMouseMove={tiltCard}
              onMouseLeave={resetTilt}
            >
              <div className="card-glow-header">
                <div className="dot" style={{ background: "#ff5f57" }} />
                <div className="dot" style={{ background: "#febc2e" }} />
                <div className="dot" style={{ background: "#28c840" }} />
              </div>
              <div className="p-4">
                <h6 className="text-dark mb-3">Performance Dashboard</h6>
                {[65, 90, 45, 78].map((w, i) => (
                  <div key={i} className="progress-bar-track mb-2">
                    <div
                      className="progress-bar-fill"
                      style={{ width: `${w}%` }}
                    />
                  </div>
                ))}
                <div className="mini-stat mt-3">
                  <span>📊 Live Analytics</span>
                  <span className="pulse-dot" />
                </div>
              </div>
            </div>
          </div>
          <div className="col-lg-6">
            <span className="section-tag">SMART DASHBOARD</span>
            <h2 className="fw-bold mt-2">See Everything, Instantly</h2>
            <p className="text-muted mb-4">
              Real-time insights into your entire workforce, powered by AI.
            </p>
            {[
              "Live attendance & leave tracking",
              "AI-generated performance reports",
              "One-click payroll approvals",
              "Custom alerts & notifications",
            ].map((p, i) => (
              <div
                key={i}
                className="showcase-point d-flex align-items-center gap-3 mb-3"
              >
                <span className="check-badge">✓</span>
                <span>{p}</span>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* 7. WHY CHOOSE */}
      <section className="why-section py-5">
        <div className="container text-center text-white reveal">
          <h2 className="fw-bold">Why Choose HRMS AI?</h2>
          <p className="mt-3 mx-auto" style={{ maxWidth: 640, opacity: 0.9 }}>
            Our AI-powered system simplifies HR operations, boosts productivity,
            and enhances employee experience — so your team can focus on people,
            not paperwork.
          </p>
        </div>
      </section>

      {/* 8. TESTIMONIALS */}
      <section className="testimonial-pin-section py-5 overflow-hidden">
        <div className="text-center mb-5 reveal">
          <span className="section-tag">TESTIMONIALS</span>
          <h2 className="fw-bold mt-2">Loved by Teams Everywhere</h2>
          <p className="text-muted">Keep scrolling →</p>
        </div>
        <div className="testimonial-track d-flex gap-4">
          {testimonials.map((t, i) => (
            <div
              key={i}
              className={`testimonial-card flex-shrink-0 ${activeTestimonial === i ? "active-testimonial" : ""}`}
              onClick={() => setActiveTestimonial(i)}
            >
              <div className="quote-mark">"</div>
              <p className="mb-3">{t.text}</p>
              <h6 className="mb-0 fw-bold">User {i + 1}</h6>
              <small className="text-muted">{t.role}</small>
            </div>
          ))}
        </div>
        <div className="d-flex justify-content-center gap-2 mt-4">
          {testimonials.map((_, i) => (
            <button
              key={i}
              className={`dot-nav ${activeTestimonial === i ? "active" : ""}`}
              onClick={() => setActiveTestimonial(i)}
            />
          ))}
        </div>
      </section>

      {/* 9. PRICING */}
      <section id="pricing" className="pricing-section container py-5">
        <div className="text-center mb-5 reveal">
          <span className="section-tag">PRICING</span>
          <h2 className="fw-bold mt-2">Simple, Transparent Pricing</h2>

          <div className="billing-toggle mt-4">
            <button
              className={
                billing === "monthly" ? "toggle-btn active" : "toggle-btn"
              }
              onClick={() => setBilling("monthly")}
            >
              Monthly
            </button>
            <button
              className={
                billing === "yearly" ? "toggle-btn active" : "toggle-btn"
              }
              onClick={() => setBilling("yearly")}
            >
              Yearly <span className="save-badge">Save 20%</span>
            </button>
          </div>
        </div>

        <div className="row g-4 justify-content-center">
          {plans.map((p, i) => {
            const price =
              p.monthly === null
                ? "Custom"
                : `₹${billing === "monthly" ? p.monthly : p.yearly}`;
            const isSelected = selectedPlan === p.name;
            return (
              // <div key={i} className="col-md-4">
              <div
                key={i}
                className="col-md-4 stack-card-mobile"
                style={{ zIndex: i + 1 }}
              >
                <div
                  className={`pricing-card h-100 ${p.popular ? "popular" : ""} ${isSelected ? "selected" : ""}`}
                  onClick={() => setSelectedPlan(p.name)}
                  onMouseMove={tiltCard}
                  onMouseLeave={resetTilt}
                  style={{ transformStyle: "preserve-3d", cursor: "pointer" }}
                >
                  {p.popular && (
                    <div className="popular-badge">Most Popular</div>
                  )}
                  {isSelected && (
                    <div className="selected-badge">✓ Selected</div>
                  )}
                  <h5 className="fw-bold">{p.name}</h5>
                  <div className="price-tag">{price}</div>
                  <small className="text-muted">{p.tag}</small>
                  <ul className="list-unstyled mt-4">
                    {p.features.map((f, j) => (
                      <li key={j} className="mb-2">
                        ✓ {f}
                      </li>
                    ))}
                  </ul>
                  <button
                    className={`ripple-btn w-100 mt-3 ${isSelected ? "btn-glow" : "btn-outline-glow-dark"}`}
                    onClick={(e) => {
                      e.stopPropagation();
                      createRipple(e);
                      setSelectedPlan(p.name);
                      showToast(`✅ ${p.name} plan selected!`);
                    }}
                  >
                    {isSelected ? "Selected ✓" : "Choose Plan"}
                  </button>
                </div>
              </div>
            );
          })}
        </div>
      </section>

      {/* 10. FAQ */}
      <section id="faq" className="container py-5">
        <div className="text-center mb-5 reveal">
          <span className="section-tag">FAQ</span>
          <h2 className="fw-bold mt-2">Got Questions?</h2>
        </div>
        <div className="faq-wrap mx-auto" style={{ maxWidth: 720 }}>
          {faqs.map((f, i) => (
            <div key={i} className="faq-item">
              <button
                className="faq-question"
                onClick={() => setActiveFaq(activeFaq === i ? null : i)}
              >
                <span>{f.q}</span>
                <span className={`faq-icon ${activeFaq === i ? "open" : ""}`}>
                  +
                </span>
              </button>
              <div className={`faq-answer ${activeFaq === i ? "open" : ""}`}>
                <p>{f.a}</p>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* CTA */}
      <section className="cta-section text-center text-white py-5">
        <div className="cta-content">
          <h2 className="fw-bold">Ready to Transform Your HR?</h2>
          <p className="mt-2 mb-4" style={{ opacity: 0.9 }}>
            Join 500+ companies already using HRMS AI.
          </p>
          <Link href="/login">
            <button
              className="btn-glow ripple-btn"
              onClick={createRipple}
              onMouseMove={magnetize}
              onMouseLeave={resetMagnet}
            >
              Start Free Trial →
            </button>
          </Link>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="bg-dark text-white py-4">
        <div className="container text-center">
          <h5>HRMS AI</h5>
          <p className="mb-0">© 2026 All Rights Reserved</p>
        </div>
      </footer>

      <style jsx global>{`
        * {
          box-sizing: border-box;
        }

        .scroll-progress {
          position: fixed;
          top: 0;
          left: 0;
          width: 100%;
          height: 4px;
          background: linear-gradient(90deg, #ffd200, #ff6fd8);
          transform-origin: left;
          transform: scaleX(0);
          z-index: 999;
        }

        .toast-box {
          position: fixed;
          bottom: 30px;
          right: 30px;
          background: #5f259f;
          color: white;
          padding: 14px 24px;
          border-radius: 10px;
          box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
          z-index: 999;
          animation: toastIn 0.4s ease;
        }
        @keyframes toastIn {
          from {
            opacity: 0;
            transform: translateY(20px);
          }
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }

        .modal-backdrop {
          position: fixed;
          inset: 0;
          background: rgba(0, 0, 0, 0.6);
          backdrop-filter: blur(4px);
          display: flex;
          align-items: center;
          justify-content: center;
          z-index: 1000;
          animation: fadeIn 0.3s ease;
        }
        @keyframes fadeIn {
          from {
            opacity: 0;
          }
          to {
            opacity: 1;
          }
        }
        .modal-box {
          background: white;
          border-radius: 20px;
          padding: 32px;
          max-width: 500px;
          width: 90%;
          position: relative;
          animation: modalPop 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
        }
        @keyframes modalPop {
          from {
            opacity: 0;
            transform: scale(0.8) translateY(30px);
          }
          to {
            opacity: 1;
            transform: scale(1) translateY(0);
          }
        }
        .modal-close {
          position: absolute;
          top: 16px;
          right: 16px;
          background: #f0eaf8;
          border: none;
          width: 32px;
          height: 32px;
          border-radius: 50%;
          font-weight: 700;
          color: #5f259f;
        }
        .modal-video-placeholder {
          background: linear-gradient(135deg, #5f259f, #7b2fc9);
          border-radius: 14px;
          height: 220px;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          color: white;
          font-size: 3rem;
          cursor: pointer;
        }

        .ripple-btn {
          position: relative;
          overflow: hidden;
        }
        .ripple {
          position: absolute;
          border-radius: 50%;
          background: rgba(255, 255, 255, 0.6);
          transform: scale(0);
          animation: rippleAnim 0.65s ease-out;
          pointer-events: none;
        }
        @keyframes rippleAnim {
          to {
            transform: scale(3);
            opacity: 0;
          }
        }

        // .navbar-glass {
        //   background: transparent;
        //   transition: all 0.35s ease;
        //   z-index: 100;
        // }
        // .navbar-glass.scrolled {
        //   background: rgba(74, 26, 122, 0.9);
        //   backdrop-filter: blur(12px);
        //   box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
        // }

        .navbar-glass {
          background: rgba(74, 26, 122, 0.35);
          backdrop-filter: blur(14px);
          -webkit-backdrop-filter: blur(14px);
          border-bottom: 1px solid rgba(255, 255, 255, 0.08);
          transition: all 0.35s ease;
          z-index: 100;
          padding: 0;
        }
        .navbar-glass.scrolled {
          background: rgba(42, 15, 71, 0.85);
          box-shadow: 0 8px 30px rgba(0, 0, 0, 0.25);
          border-bottom: 1px solid rgba(255, 209, 0, 0.15);
        }

        .logo-group {
          position: relative;
        }
        .logo-icon {
          width: 38px;
          height: 38px;
          border-radius: 10px;
          background: linear-gradient(135deg, #ffd200, #ff6fd8);
          display: flex;
          align-items: center;
          justify-content: center;
          font-weight: 800;
          color: #4a1a7a;
          // font-size: 1.1rem;
          font-size: 0.8rem;
          box-shadow: 0 4px 15px rgba(255, 210, 0, 0.35);
          transition: transform 0.3s ease;
        }
        .logo-group:hover .logo-icon {
          transform: rotate(-8deg) scale(1.08);
        }
        .logo-text {
          letter-spacing: -0.3px;
        }

        .nav-links-wrap {
          background: rgba(255, 255, 255, 0.06);
          border: 1px solid rgba(255, 255, 255, 0.08);
          border-radius: 999px;
          padding: 6px 8px;
        }

        .nav-link-glow {
          position: relative;
          color: rgba(255, 255, 255, 0.8);
          text-decoration: none;
          font-size: 0.92rem;
          font-weight: 500;
          padding: 8px 16px;
          border-radius: 999px;
          transition:
            color 0.25s ease,
            background 0.25s ease;
        }

        .nav-link-glow::after {
          content: "";
          position: absolute;
          left: 16px;
          right: 16px;
          bottom: 5px;
          height: 2px;
          background: linear-gradient(90deg, #ffd200, #ff6fd8);
          transform: scaleX(0);
          transform-origin: left;
          transition: transform 0.3s ease;
          border-radius: 2px;
        }
        .nav-link-glow:hover {
          color: white;
          background: rgba(255, 255, 255, 0.08);
        }

        .nav-link-glow:hover::after {
          transform: scaleX(1);
        }

        .login-link {
          color: rgba(255, 255, 255, 0.85);
          font-weight: 600;
          font-size: 0.92rem;
          cursor: pointer;
          transition: color 0.25s ease;
        }
        .login-link:hover {
          color: #ffd200;
        }

        .btn-glow-sm {
          background: linear-gradient(90deg, #ffd200, #ffb100);
          color: #4a1a7a;
          border: none;
          padding: 10px 22px;
          border-radius: 999px;
          font-weight: 700;
          font-size: 0.9rem;
          box-shadow: 0 4px 15px rgba(255, 210, 0, 0.3);
          transition:
            transform 0.25s ease,
            box-shadow 0.25s ease;
        }
        .btn-glow-sm:hover {
          transform: translateY(-2px);
          box-shadow: 0 8px 22px rgba(255, 210, 0, 0.45);
        }

        .mobile-login {
          display: block;
          padding: 12px 24px;
          color: #ffd200;
          font-weight: 700;
        }

        .hamburger {
          display: flex;
          flex-direction: column;
          gap: 5px;
          background: none;
          border: none;
          padding: 6px;
          z-index: 200;
        }
        .bar {
          width: 24px;
          height: 2px;
          background: white;
          transition: all 0.3s ease;
        }
        .bar1.open {
          transform: translateY(7px) rotate(45deg);
        }
        .bar2.open {
          opacity: 0;
        }
        .bar3.open {
          transform: translateY(-7px) rotate(-45deg);
        }

        .mobile-menu {
          max-height: 0;
          overflow: hidden;
          background: rgba(74, 26, 122, 0.97);
          transition: max-height 0.4s ease;
          display: flex;
          flex-direction: column;
        }
        .mobile-menu.open {
          max-height: 300px;
          padding: 10px 0;
        }
        .mobile-menu a {
          color: white;
          padding: 12px 24px;
          text-decoration: none;
          border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .hero-section {
          min-height: 95vh;
          background: linear-gradient(
            135deg,
            #4a1a7a 0%,
            #5f259f 45%,
            #7b2fc9 100%
          );
          padding-bottom: 100px;
        }
        .blob {
          position: absolute;
          border-radius: 50%;
          filter: blur(60px);
          opacity: 0.35;
        }
        .blob-1 {
          width: 350px;
          height: 350px;
          background: #ffd200;
          top: -60px;
          right: 10%;
        }
        .blob-2 {
          width: 280px;
          height: 280px;
          background: #ff6fd8;
          bottom: -60px;
          left: 5%;
        }
        .blob-3 {
          width: 200px;
          height: 200px;
          background: #00e5ff;
          top: 40%;
          right: 30%;
        }

        .badge-pill {
          display: inline-block;
          background: rgba(255, 255, 255, 0.15);
          border: 1px solid rgba(255, 255, 255, 0.3);
          padding: 6px 16px;
          border-radius: 999px;
          font-size: 0.85rem;
          backdrop-filter: blur(6px);
        }
        .hero-title {
          font-size: 2.8rem;
          font-weight: 800;
          line-height: 1.2;
        }
        @media (min-width: 992px) {
          .hero-title {
            font-size: 3.4rem;
          }
        }
        .gradient-text {
          background: linear-gradient(90deg, #ffd200, #ff6fd8);
          -webkit-background-clip: text;
          background-clip: text;
          color: transparent;
        }

        .btn-glow {
          background: linear-gradient(90deg, #ffd200, #ffb100);
          color: #4a1a7a;
          border: none;
          padding: 14px 30px;
          border-radius: 10px;
          font-weight: 700;
          animation: glowPulse 2.4s ease-in-out infinite;
        }
        @keyframes glowPulse {
          0%,
          100% {
            box-shadow: 0 0 0px rgba(255, 210, 0, 0.5);
          }
          50% {
            box-shadow: 0 0 25px rgba(255, 210, 0, 0.7);
          }
        }
        .btn-outline-glow {
          background: transparent;
          border: 1.5px solid rgba(255, 255, 255, 0.5);
          color: white;
          padding: 14px 28px;
          border-radius: 10px;
          font-weight: 600;
        }
        .btn-outline-glow:hover {
          background: rgba(255, 255, 255, 0.1);
          border-color: white;
        }
        .btn-outline-glow-dark {
          background: transparent;
          border: 1.5px solid #5f259f;
          color: #5f259f;
          padding: 12px 24px;
          border-radius: 10px;
          font-weight: 600;
          transition: all 0.25s;
        }
        .btn-outline-glow-dark:hover {
          background: #5f259f;
          color: white;
        }

        .floating-card {
          background: white;
          border-radius: 16px;
          box-shadow: 0 20px 50px rgba(0, 0, 0, 0.25);
          will-change: transform;
        }
        .card-main {
          max-width: 340px;
          margin-left: auto;
          margin-right: 40px;
          overflow: hidden;
        }
        .card-mini {
          max-width: 200px;
          margin-left: auto;
          margin-top: -30px;
          margin-right: 10px;
          padding: 14px 18px;
        }
        .card-glow-header {
          display: flex;
          gap: 6px;
          padding: 12px 16px;
          background: #f5f5f7;
        }
        .card-glow-header .dot {
          width: 10px;
          height: 10px;
          border-radius: 50%;
        }
        .progress-bar-track {
          height: 8px;
          background: #eee;
          border-radius: 999px;
          overflow: hidden;
        }
        .progress-bar-fill {
          height: 100%;
          background: linear-gradient(90deg, #5f259f, #9c4dcc);
          transition: width 1s ease;
        }
        .mini-stat {
          display: flex;
          align-items: center;
          justify-content: space-between;
          background: #f8f5ff;
          padding: 8px 12px;
          border-radius: 8px;
          font-size: 0.85rem;
        }
        .pulse-dot {
          width: 10px;
          height: 10px;
          border-radius: 50%;
          background: #28c840;
          animation: pulseDot 1.6s infinite;
        }
        @keyframes pulseDot {
          0% {
            box-shadow: 0 0 0 0 rgba(40, 200, 64, 0.6);
          }
          70% {
            box-shadow: 0 0 0 10px rgba(40, 200, 64, 0);
          }
          100% {
            box-shadow: 0 0 0 0 rgba(40, 200, 64, 0);
          }
        }
        .wave-bottom {
          position: absolute;
          bottom: -1px;
          left: 0;
          width: 100%;
          height: 100px;
        }

        .stats-strip {
          background: white;
          border-radius: 20px;
          box-shadow: 0 15px 50px rgba(95, 37, 159, 0.12);
          margin-top: -70px;
          position: relative;
          z-index: 3;
          padding: 40px 20px;
        }

        .marquee-wrap {
          overflow: hidden;
        }
        .marquee-track {
          display: flex;
          gap: 60px;
          width: max-content;
        }
        .marquee-item {
          font-size: 1.3rem;
          font-weight: 700;
          color: #c9b8e0;
          white-space: nowrap;
        }

        .section-tag {
          color: #5f259f;
          font-weight: 700;
          letter-spacing: 1.5px;
          font-size: 0.8rem;
        }

        .feature-card {
          background: white;
          border-radius: 16px;
          padding: 30px 24px;
          border: 1px solid #f0eaf8;
          transition:
            box-shadow 0.3s,
            border-color 0.3s;
          cursor: pointer;
          will-change: transform;
        }
        .feature-card:hover {
          box-shadow: 0 20px 40px rgba(95, 37, 159, 0.15);
          border-color: #d9c6f2;
        }
        .feature-icon {
          font-size: 2rem;
          width: 60px;
          height: 60px;
          display: flex;
          align-items: center;
          justify-content: center;
          background: linear-gradient(135deg, #f3ebff, #eaf6ff);
          border-radius: 14px;
        }

        .phonepe-section {
          background: radial-gradient(
            circle at center,
            #3a1568 0%,
            #1a0b33 75%
          );
          height: 100vh;
          width: 100%;
          position: fixed;
          overflow: hidden;

        }

        .phonepe-pin-inner {
          position: absolute;
          top: 50%;
          left: 50%;
          transform: translate(-50%, -50%);
          width: 100%;
          display: flex;
          align-items: center;
          justify-content: center;
          z-index: 2;
        }

        .phonepe-rings {
          position: absolute;
          top: 50%;
          left: 50%;
          width: 0;
          height: 0;
          z-index: 1;
          pointer-events: none;
        }
        .ring {
          position: absolute;
          top: 0;
          left: 0;
          border: 1px solid rgba(255, 255, 255, 0.12);
          border-radius: 50%;
          width: 200px;
          height: 200px;
          transform: translate(-50%, -50%) scale(0.4);
          animation: ringPulse 5s ease-out infinite;
        }
        .ring-1 {
          animation-delay: 0s;
        }
        .ring-2 {
          animation-delay: 1.25s;
        }
        .ring-3 {
          animation-delay: 2.5s;
        }
        .ring-4 {
          animation-delay: 3.75s;
        }
        @keyframes ringPulse {
          0% {
            transform: translate(-50%, -50%) scale(0.4);
            opacity: 0.9;
          }
          100% {
            transform: translate(-50%, -50%) scale(5);
            opacity: 0;
          }
        }

        .morph-shape {
          position: relative;
          background: rgba(255, 255, 255, 0.06);
          border: 1px solid rgba(255, 255, 255, 0.15);
          backdrop-filter: blur(14px);
          -webkit-backdrop-filter: blur(14px);
          overflow: hidden;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          padding: 30px 20px;
          will-change: width, height, border-radius;
        }

        .morph-title {
          text-align: center;
          margin-bottom: 30px;
        }

        .cards-viewport {
          width: 100%;
          max-width: 900px;
          overflow: hidden;
        }

        .cards-track {
          display: flex;
          gap: 24px;
          will-change: transform;
        }

        .phonepe-card {
          flex: 0 0 260px;
          background: rgba(255, 255, 255, 0.07);
          border: 1px solid rgba(255, 255, 255, 0.14);
          border-radius: 18px;
          padding: 24px;
          transition:
            transform 0.3s ease,
            background 0.3s ease;
        }
        .phonepe-card:hover {
          transform: translateY(-6px);
          background: rgba(255, 255, 255, 0.12);
        }
        .phonepe-card-number {
          font-size: 1.5rem;
          font-weight: 800;
          color: #ffd200;
        }
        .phonepe-card-desc {
          color: rgba(255, 255, 255, 0.7);
          font-size: 0.88rem;
        }

        @media (max-width: 767px) {
          .phonepe-card {
            flex: 0 0 220px;
            padding: 18px;
          }
          .morph-title h2 {
            font-size: 1.4rem;
          }
          .cards-viewport {
            max-width: 100%;
            padding: 0 10px;
          }
        }
        .step-card {
          background: rgba(255, 255, 255, 0.06);
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 16px;
          padding: 28px;
          height: 100%;
          transition:
            transform 0.3s ease,
            background 0.3s;
        }
        .step-card:hover {
          transform: translateY(-6px);
          background: rgba(255, 255, 255, 0.1);
        }
        .step-number {
          font-size: 2.2rem;
          font-weight: 800;
          color: #ffd200;
          opacity: 0.8;
        }

        .check-badge {
          width: 26px;
          height: 26px;
          border-radius: 50%;
          background: #e8dcfb;
          color: #5f259f;
          display: flex;
          align-items: center;
          justify-content: center;
          font-weight: 700;
          flex-shrink: 0;
        }

        .why-section {
          background: linear-gradient(135deg, #5f259f, #7b2fc9);
        }

        .testimonial-pin-section {
          background: #faf7ff;
        }
        .testimonial-track {
          padding: 0 20px;
        }
        .testimonial-card {
          background: white;
          border-radius: 16px;
          padding: 32px;
          width: 340px;
          box-shadow: 0 10px 30px rgba(95, 37, 159, 0.1);
          cursor: pointer;
          transition:
            transform 0.3s,
            box-shadow 0.3s;
        }
        .testimonial-card.active-testimonial {
          transform: scale(1.05);
          box-shadow: 0 20px 40px rgba(95, 37, 159, 0.25);
          border: 2px solid #5f259f;
        }
        .quote-mark {
          font-size: 3rem;
          color: #d9c6f2;
          line-height: 0.5;
          margin-bottom: 10px;
        }
        .dot-nav {
          width: 10px;
          height: 10px;
          border-radius: 50%;
          background: #d9c6f2;
          border: none;
          transition: all 0.3s;
        }
        .dot-nav.active {
          background: #5f259f;
          width: 26px;
          border-radius: 6px;
        }

        .billing-toggle {
          display: inline-flex;
          background: #f0eaf8;
          padding: 6px;
          border-radius: 999px;
          gap: 4px;
        }
        .toggle-btn {
          border: none;
          background: transparent;
          padding: 10px 22px;
          border-radius: 999px;
          font-weight: 600;
          color: #5f259f;
          transition: all 0.3s;
          position: relative;
        }
        .toggle-btn.active {
          background: #5f259f;
          color: white;
        }
        .save-badge {
          background: #ffd200;
          color: #4a1a7a;
          font-size: 0.65rem;
          padding: 2px 6px;
          border-radius: 999px;
          margin-left: 4px;
          font-weight: 700;
        }

        .pricing-card {
          background: white;
          border-radius: 18px;
          padding: 32px;
          border: 1px solid #eee;
          text-align: center;
          position: relative;
          will-change: transform;
          transition:
            border-color 0.3s,
            box-shadow 0.3s;
        }
        .pricing-card.popular {
          border: 2px solid #5f259f;
          box-shadow: 0 20px 50px rgba(95, 37, 159, 0.2);
        }
        .pricing-card.selected {
          border: 2px solid #28c840;
          box-shadow: 0 20px 50px rgba(40, 200, 64, 0.2);
        }
        .popular-badge {
          position: absolute;
          top: -14px;
          left: 50%;
          transform: translateX(-50%);
          background: #ffd200;
          color: #4a1a7a;
          padding: 4px 14px;
          border-radius: 999px;
          font-size: 0.75rem;
          font-weight: 700;
        }
        .selected-badge {
          position: absolute;
          top: -14px;
          right: 16px;
          background: #28c840;
          color: white;
          padding: 4px 12px;
          border-radius: 999px;
          font-size: 0.7rem;
          font-weight: 700;
        }
        .price-tag {
          font-size: 2.4rem;
          font-weight: 800;
          color: #5f259f;
          margin: 8px 0;
          transition: all 0.3s;
        }

        .faq-wrap .faq-item {
          border-bottom: 1px solid #eee;
        }
        .faq-question {
          width: 100%;
          background: none;
          border: none;
          padding: 20px 0;
          display: flex;
          justify-content: space-between;
          align-items: center;
          font-weight: 600;
          text-align: left;
          font-size: 1.05rem;
        }
        .faq-icon {
          font-size: 1.4rem;
          color: #5f259f;
          transition: transform 0.3s;
        }
        .faq-icon.open {
          transform: rotate(45deg);
        }
        .faq-answer {
          max-height: 0;
          overflow: hidden;
          transition:
            max-height 0.4s ease,
            padding 0.4s ease;
        }
        .faq-answer.open {
          max-height: 200px;
          padding-bottom: 20px;
        }
        .faq-answer p {
          color: #777;
          margin: 0;
        }

        .cta-section {
          background: linear-gradient(135deg, #7b2fc9, #5f259f);
        }

        /* ===== MOBILE RESPONSIVE FIXES ===== */
        @media (max-width: 767px) {
          .container {
            padding-left: 20px;
            padding-right: 20px;
          }

          .logo-icon {
            width: 32px;
            height: 32px;
            font-size: 0.64rem;
          }
          .logo-text {
            font-size: 1.1rem !important;
          }

          .hero-section {
            min-height: auto;
            padding-top: 40px;
            padding-bottom: 120px;
          }
          .hero-title {
            font-size: 2rem !important;
            line-height: 1.25;
          }
          .hero-sub {
            font-size: 0.95rem;
          }
          .badge-pill {
            font-size: 0.75rem;
            padding: 5px 12px;
          }

          .hero-cta-group {
            flex-direction: column;
            align-items: stretch;
          }
          .hero-cta-group button {
            width: 100%;
            justify-content: center;
          }

          .stats-strip {
            margin-top: 30px;
            padding: 24px 16px;
            border-radius: 16px;
          }
          .stats-strip .counter {
            font-size: 1.6rem !important;
          }
          .stats-strip p {
            font-size: 0.75rem;
          }

          .mobile-cta-wrap {
            padding: 16px 24px;
          }

          .feature-card {
            padding: 22px 18px;
          }
          .step-card {
            padding: 20px;
          }

          .pricing-card {
            padding: 24px 20px;
          }
          .price-tag {
            font-size: 1.9rem;
          }

          .testimonial-card {
            width: 280px;
            padding: 24px;
          }

          .section-tag {
            font-size: 0.7rem;
          }

          // for overlap animation
          .stagger-group {
            position: relative;
          }

          .stack-card-mobile {
            position: sticky;
            top: 78px; /* apne navbar ki height ke hisaab se adjust karo */
            padding-bottom: 0 !important;
          }

          .stack-card-mobile .feature-card,
          .stack-card-mobile .step-card,
          .stack-card-mobile .pricing-card {
            min-height: 220px;
            border-radius: 24px !important;
            box-shadow: 0 -10px 40px rgba(0, 0, 0, 0.15);
            display: flex;
            flex-direction: column;
            justify-content: center;
          }

          .stack-card-mobile .step-card {
            background: linear-gradient(135deg, #3a1568, #5f259f);
            min-height: 180px;
          }

          .stack-card-mobile .pricing-card {
            min-height: 380px;
          }

          /* Har card ke baad thoda extra scroll space taaki reveal smooth lage */
          .stack-card-mobile:not(:last-child) {
            margin-bottom: 40px;
          }
        }

        @media (max-width: 991px) {
          .hero-title {
            font-size: 2.6rem;
          }
        }
      `}</style>
    </div>
  );
}
